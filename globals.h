#pragma once
#include<SFML\Graphics.hpp>

//18 tiles vertically
const sf::Vector2f tileSize = sf::Vector2f(60, 60);
const sf::Vector2f windowSize = sf::Vector2f(1920, 1080);
const unsigned int msFrameTime = 11;